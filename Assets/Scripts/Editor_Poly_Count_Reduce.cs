using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

/*
[CustomEditor(typeof(Poly_Count_Reduce))]
public class Editor_Poly_Count_Reduce : Editor
{

    void OnSceneGUI()
    {
        //MeshCombiner mc = target as MeshCombiner;
        Poly_Count_Reduce sp = target as Poly_Count_Reduce;

        if (Handles.Button(sp.transform.position + Vector3.up * 5, Quaternion.LookRotation(Vector3.up), 1, 1, Handles.CylinderHandleCap))
        {
            sp.OptimizarMesh();
        }
    }
}*/
